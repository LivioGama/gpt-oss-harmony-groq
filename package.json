{
  "name": "gpt-oss-harmony-groq",
  "version": "1.0.0",
  "description": "Transformers-based proxy for gpt-oss-20b with Groq API integration and harmony format support",
  "type": "module",
  "scripts": {
    "dev": "lsof -ti:3307 | xargs kill -9 && bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target node",
    "test": "bun test"
  },
  "dependencies": {
    "@types/node": "^20.0.0",
    "openai-harmony": "^0.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
